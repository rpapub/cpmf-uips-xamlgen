def generate(
    ast: dict,
    engine_results: list[dict],
    policy_file: str | None = None,
    verbose: bool = False,
) -> dict:
    """Generate XAML from an input AST. Returns output envelope. Null-op stub."""
    return None
